# ===========================================
# DATABASE (SUPABASE + PRISMA)
# ===========================================
# Obtenha estes valores em: Supabase → Project Settings → Database → Connection string

# Connection Pooler (para Vercel e produção)
DATABASE_URL="postgresql://postgres.[projeto-id]:[senha]@aws-0-us-east-1.pooler.supabase.com:5432/postgres?pgbouncer=true"

# Direct Connection (para migrations)
DIRECT_URL="postgresql://postgres.[projeto-id]:[senha]@aws-0-us-east-1.compute.amazonaws.com:5432/postgres"

# ===========================================
# AUTENTICAÇÃO
# ===========================================
# Gere uma chave secreta forte: openssl rand -base64 32
JWT_SECRET="sua_chave_secreta_aqui"

# ===========================================
# MELHOR ENVIO API
# ===========================================
# Para usar a API real do Melhor Envio:
# 1. Crie uma conta em https://melhorenvio.com.br
# 2. Acesse o painel e gere um token de API
# 3. Copie o token e cole abaixo
# 4. Remova o comentário (#) da linha

# VITE_MELHOR_ENVIO_TOKEN=seu_token_aqui

# Use sandbox para testes (true) ou produção (false)
# VITE_MELHOR_ENVIO_SANDBOX=true

# NOTA: Se não configurar o token, o sistema usará cálculos estimados
# baseados em fórmulas reais dos Correios (2024)
